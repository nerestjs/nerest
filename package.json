{
  "name": "@nerest/nerest",
  "version": "0.0.1",
  "description": "TypeScript micro frontend framework",
  "scripts": {
    "build": "tsc -p tsconfig.json -d"
  },
  "prettier": "@tinkoff/prettier-config",
  "devDependencies": {
    "@tinkoff/prettier-config": "^1.32.1",
    "typescript": "^4.8.4"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
